safetensors>=0.4.0
numpy>=1.24.0
huggingface_hub>=0.20.0
requests>=2.25.0
# matplotlib is optional (for figure generation); install explicitly if needed:
# pip install matplotlib
# gguf-py: pip install -e ./ggml/gguf-py
